apiVersion: v1
kind: Pod
metadata:
  name: '{{ include "lgtm-helm-chart.fullname" . }}-test-connection'
  labels:
    {{- include "lgtm-helm-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lgtm-helm-chart.fullname" . }}-app:{{ .Values.api.service.port }}']
  restartPolicy: Never
