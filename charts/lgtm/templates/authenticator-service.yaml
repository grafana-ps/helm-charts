apiVersion: v1
kind: Service
metadata:
  name: {{ include "lgtm-helm-chart.fullname" . }}-authenticator
  labels:
    {{- include "lgtm-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.authenticator.service.type }}
  ports:
    - port: {{ .Values.authenticator.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "lgtm-helm-chart.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: authenticator
